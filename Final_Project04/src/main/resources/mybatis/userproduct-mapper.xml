<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="userproduct">

	<select id="userProductList" resultType="userproductDto">
		SELECT *
		FROM Product
		WHERE userRole = 'USER'
		ORDER BY productRegDate DESC
	</select>
	
	<select id="userProductOne" resultType="userproductDto">
		SELECT *
		FROM Product
		WHERE productSeq = #{productSeq}
	</select>
	
	<insert id="userProductInsert" parameterType="userproductDto">
		INSERT INTO Product
		VALUES (productSeq.NEXTVAL, #{productName}, #{productPrice}, #{sellerAddress}, SYSDATE, #{productInfo}, NULL, NULL, 'Y', 'USER', 1, 'test', #{userLatitude}, #{userLongitude})
	</insert>
	
	
	<!-- 지역으로 물품 검색 -->
	<select id="selectByaddr" parameterType="String" resultType="userproductDto">
		SELECT * FROM Product WHERE sellerAddress LIKE '%#{addr}%' ORDER BY productRegDate DESC
	</select>
</mapper>
